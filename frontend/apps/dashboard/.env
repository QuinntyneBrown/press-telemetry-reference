# Base URL for the Telemetry API. Empty = same-origin (dev server proxies /api and
# /hubs to the backend). Override at build/dev time per L2-018, e.g.
# VITE_API_BASE_URL=http://localhost:5063
VITE_API_BASE_URL=
